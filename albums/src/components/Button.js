import React from 'react';
import {Text, TouchableOpacity} from 'react-native';

const Button = () => {
  return (
    <TouchableOpacity>
      <Text>Click Meh</Text>
    </TouchableOpacity>
  );
};

export default Button;